$kind: collection
scripts:
  - type: http:afterResponse
    language: text/javascript
    code: |-
      // Folder-level: System — runs after every request in 09 System
      pm.test('System endpoint does not return a server error (5xx)', function () {
          pm.expect(pm.response.code).to.be.below(500);
      });
