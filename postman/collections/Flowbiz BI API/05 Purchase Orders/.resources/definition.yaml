$kind: collection
scripts:
  - type: http:afterResponse
    language: text/javascript
    code: |-
      // Folder-level: Purchase Orders — runs after every request in 05 Purchase Orders
      pm.test('Purchase Orders endpoint does not return a server error (5xx)', function () {
          pm.expect(pm.response.code).to.be.below(500);
      });
